describe('Pizza', function() {
  it("creates a new pizza with listed, quanity size and toppings", function() {
    var quantity = 1;
    var pizzaSize
    var toppings
    var testPizza
    expect().to.eql([]);
    expect().to.eql([]);
  });

  it("calculates the cost of a pizza based on what was selected", function() {
   var quantity = 1;
   var pizzaSize
   var toppings
   var testPizza
   expect().to.eql([]);
   expect().to.eql([]);
 });
});

sep test for pizza topping


sep test for pizza size 
